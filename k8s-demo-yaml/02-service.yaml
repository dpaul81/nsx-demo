apiVersion: v1
kind: Service
metadata:
        name: nsx-demo
        labels:
                app: nsx-demo
spec:
        ports:
                - port: 8080
        selector:
                app: nsx-demo
        type: LoadBalancer
